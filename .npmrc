# NPM Configuration for Vercel Deployment
# Handle peer dependency conflicts gracefully

# Use legacy peer dependencies resolution
legacy-peer-deps=true

# Optionally use exact package versions
# save-exact=true

# Engine strict mode (optional)
# engine-strict=true
